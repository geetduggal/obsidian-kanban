Card ID feature update:
- add configurable card IDs with unified ID + menu pill rendering
- add ID generation modes (random letters recommended) and adaptive random length
- add archive behavior option to keep/drop IDs on archive
- add commands to assign missing IDs and regenerate all IDs
- improve visual pill styling and positioning for faster scanning

aed9f48 Add blank line splitting for root lists without headings
de8e18f Replace kanban:settings syntax with YAML frontmatter and change default items to plain dash
